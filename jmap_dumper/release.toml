pre-release-replacements = [
  { file = "../CHANGELOG.md", search = "## Unreleased", replace = "## v{{version}} - {{date}}", exactly = 1 },
  { file = "../CHANGELOG.md", search = "<!-- next-header -->", replace = "<!-- next-header -->\n\n## Unreleased", exactly = 1 },
]
